# 腾讯面经 3

## Q1. 如果我们要去设计一套高并发的票务系统，如何去确保这个系统的高吞吐，并且确保它的稳定性和票不能够超卖？对于这么一个产品，你要怎么去设计？

我的思路是：层层拦截流量，把真正需要处理的请求降到最低；用 Redis + Lua 做原子库存扣减防超卖；用 MQ 异步削峰保护数据库。

```
开票前：
  ① CDN 缓存静态页面
  ② 库存预热到 Redis
  ③ 等候室排队，过滤流量

开票瞬间：
  ④ 网关限流（令牌桶），超出直接拒绝
  ⑤ 用户去重，1秒内只处理一次

抢票核心：
  ⑥ Lua 脚本原子扣减 Redis 库存（第一道防线）
  ⑦ 发 MQ 消息，立刻返回"抢票成功"

异步处理：
  ⑧ 消费 MQ，幂等创建订单
  ⑨ 数据库乐观锁 + 唯一索引（第二三道防线）

后续：
  ⑩ 用户支付，更新订单状态
  ⑪ 15分钟未支付，自动取消，归还库存
```

---

## Q5. 你提到在前端调度上要做 CDN，这是为了做什么？

用户打开票务网站，浏览器要加载：

- 活动页面的 HTML
- CSS 样式文件
- JavaScript 文件
- 活动图片（可能很大）
- 歌手照片、演出海报...

**没有 CDN：**

```
全国所有用户的这些请求都打到你的服务器
100万人同时打开页面
100万个"给我图片"的请求打到北京的服务器
服务器带宽直接被图片请求占满
真正的抢票请求反而进不来！
```

**CDN 的做法：**

在全国各地部署很多节点（边缘服务器），把静态资源（图片、JS、CSS）缓存到这些节点。

```
CDN = 在用户附近缓存静态资源
      把"给我图片/JS/CSS"这类请求
      拦截在离用户最近的节点解决
      不让这些请求打到源服务器
```

---

## Q7. 请求到网关以后，你提到了限流，大概来讲一下有哪些限流的方式方法？

---

## Q9. 为什么要考虑异步的方式？（指抢购）

---

## Q11. 如果用异步处理，数据库接收的请求会不会变少？那我们如何去保证它的高吞吐？假设1秒钟有1万个请求，如果是用异步的方式，是不是1万个请求都要在队列里排队执行？

多消费者并行消费 + 批量写入

---

## Q13. 在异步之前有一个缓存这一层，缓存这一层你是怎么去设计的？它如何去确保不超卖的行为？

## Q14. 缓存和数据库会不会存在最终不一致的情况？例如网络中断、退票或支付超时，怎么去确保缓存里的库存和数据库最终会保证一致？

## Q15. 布隆过滤器有没有了解？

## Q16. 缓存击穿和缓存雪崩这两个场景，我们一般是怎么去解决的？

## Q17. 介绍一下分布式锁，我们一般是用它去解决哪类问题？它的实现方式大概有哪些？

## Q18. 在消息队列里，我们是如何去确保消息的可靠性的？

## Q19. 在哪些场景下，我们不会使用 ACK 确认机制？

## Q20. 消息消费失败的话，一般会怎么去处理？

## Q21. 用消息队列时有一个常见问题是幂等性问题，我们一般怎么去解决，确保程序是幂等的？

## Q22. 你提到会把消息的唯一 ID 放到 Redis 里，这个 key 会一直存在吗？什么时候删除？过期时间怎么设置？

## Q23. 如果在数据库层面做幂等，你要怎么去设计表？

## Q24. 在 Java 里我们会用到 AOP 思想，你大概解释一下什么是 AOP，以及我们会在什么场景下使用？

## Q25. 你用过 Redis，大概解释一下 Redisson 实现分布式锁的原理。

## Q26. Java 里面内存是怎么管理的？不同类型的变量存储在哪里？它们的生命周期又是怎样的？

## Q27. 在程序开发时，会出现栈溢出或堆溢出的情况，你能够解释一下在哪些情况下会出现，以及如何去解决？

## Q28. 在线程池里做开发时，如何去设置线程池的数量？

## Q29. 做项目时是怎么考虑系统安全性的？例如怎么识别用户身份，怎么判断用户权限？

## Q30. 你对比一下 Cookie Session 和 JWT 这两种方式的优缺点。

## Q31. JWT 这边，如何去处理它过期的问题？

## Q32. 如何去让用户注销，也就是退出系统？

## Q33. JWT 是怎么颁发给前端的？服务器怎么去颁发一个 JWT？

## Q34. JWT 是否可能会被篡改？如何防止被篡改？

## Q35. 你对 OAuth2.0 或 SAML2.0 这两个协议有了解吗？

## Q36. 权限方面你会怎么去设计？

## Q37. 假设需要对现在的项目做优化，需要考虑角色，登录后某些人只能访问某些接口，其他人全部接口都能访问，你要怎么去设计这个功能？

## Q38. 你对微服务的设计有没有了解？

## Q39. 你对 K8s 有没有了解？（不了解）

## Q40. 你对服务的注册和发现有没有了解？

## Q41. 你这边有没有什么问题想问我的？
